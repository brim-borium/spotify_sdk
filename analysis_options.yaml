include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Ignore generated files
    - "**/*.g.dart"
    - "lib/generated/**"
    - "lib/models/**"
  errors:
    close_sinks: ignore
linter:
  rules:
    unsafe_html: false
