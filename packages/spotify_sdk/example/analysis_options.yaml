# This file configures the analyzer for the example app.
# It inherits the linting rules from the parent spotify_sdk plugin.
analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
include: package:very_good_analysis/analysis_options.yaml
